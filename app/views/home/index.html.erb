

<% if user_signed_in? %>

Enter keywords you'd like to recieve updates on here. <br>
<% current_user.keywords.map{|keyword|keyword.name}.join(" • ") %>

<div class="keyword_list"><strong><%= current_user.keywords.map{|keyword|keyword.name}.join(' • ') %></strong></div>
<br>
<%= form_for @keyword, remote: true do |f| %>
<%= f.text_field :name %>
<%= f.submit "Add Keyword" %>
<% end %>
<br>
<br>
Feed

<% end %>

